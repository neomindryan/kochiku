%table.worker-health
  %thead
    %th.right{:style => "width=100%"} Worker
    %th.left Status
  %tbody
    - @workers.each do |worker,status|
      %tr
        %td.right= worker
        %td
          - status.take(15).each do |state|
            %span.attempt-status{:class => state}
